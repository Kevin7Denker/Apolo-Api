{
  "version": 2,
  "builds": [
    {
      "src": "ts-node src/server.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/auth/(.*)",
      "dest": "/auth/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
